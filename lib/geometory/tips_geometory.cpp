#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main() {
    // 度数 to radian
    double d = 90;
    double rad = M_PI * d / 180;

    // radian to 度数
    double r = M_PI / 2;
    double dosu = 180 * r / M_PI;

    return 0;
}